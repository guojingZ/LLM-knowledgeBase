# 知识元模型建模与萃取规范指南

> **用途**：本规范定义如何从个人历史文章库中萃取结构化知识，构建"场景 → 概念 → 实体"三层元模型。同时包含建模规范（给设计师）和应用指南（给 AI 操作的提示词模板）。
>
> **版本**：2.0
>
> **前置阅读**：本规范是基于 wiki1 实践经验（445 篇文章 → 665 原子概念 + 111 组合概念 + 85 场景 + 169 实体）的反思与升级，核心改进方向为：**简化结构、严格去重、两轮迭代**。

---

## 目录

1. [知识元模型架构](#一知识元模型架构)
2. [YAML 文件 Schema 定义](#二yaml-文件-schema-定义)
3. [关系类型定义](#三关系类型定义)
4. [去重规范](#四去重规范)
5. [两轮萃取流程](#五两轮萃取流程)
6. [建模规范指南](#六建模规范指南)
7. [应用指南（提示词模板）](#七应用指南提示词模板)

---

## 一、知识元模型架构

### 1.1 核心思想

知识元模型是一套**面向场景组装**的结构化知识表示系统。它从个人历史文章中萃取核心要素，按三层架构组织：

```
┌──────────────────────────────────────────────────┐
│              场景层 (Scenarios)                   │
│  "我要解决什么问题"                                │
│  每个场景定义：触发条件 → 组装规则 → 产出目标       │
└──────────────────────────────────────────────────┘
                      │ 调用（组合关系）
                      ↓
┌──────────────────────────────────────────────────┐
│              概念层 (Concepts)                    │
│  "解决这个问题需要什么方法/思维模型"                │
│  每个概念定义：定义 + IPO模型 + 子概念分解 + 关系   │
└──────────────────────────────────────────────────┘
                      │ 使用/依赖/引用/输出
                      ↓
┌──────────────────────────────────────────────────┐
│              实体层 (Entities)                    │
│  "过程中涉及哪些具体的人、工具、产品、框架"         │
│  每个实体定义：类型 + 关系网络 + 父子层级           │
└──────────────────────────────────────────────────┘
```

**类比理解**：如果把解决一个问题比作做一道菜：

| 层 | 类比 | 例子 |
|---|---|---|
| **场景** | 菜谱 | "如何做一道川味宫保鸡丁"——定义了什么时候做、按什么步骤、产出什么 |
| **概念** | 烹饪技法 | "爆炒""腌制""勾芡"——有明确的输入(生食材)、处理过程(火候/时间/手法)、输出(半成品或成品) |
| **实体** | 食材与工具 | 鸡胸肉、花生米、郫县豆瓣酱、炒锅——是具体可指认的人/物/产品/框架 |

### 1.2 三层各自定位

| 层 | 本质 | 判别标准 | 典型示例 |
|---|---|---|---|
| **场景** | 问题导向的方法论 | 以"如何…"开头，有明确的 trigger 和 goal | 如何进行深度思考、如何构建个人知识体系 |
| **概念** | 可复用的思维加工单元 | 有 IPO（输入→处理→输出）或可分解为子步骤 | 第一性原理、MECE原则、结构化思维 |
| **实体** | 具体的人/物/工具/框架/产品 | 是现实中存在或可指认的具体对象 | 马斯克、Cursor、SBR模型、麦肯锡七步法 |

### 1.3 概念的两类建模方式

一个概念可以同时具备 IPO 和 decomposition，也可以只有其一：

**方式 A：IPO 模型（原子性概念）**

适用于自身足够原子化、不需要拆解为其他概念的方法/原理。用 `ipo` 字段建模：

```
概念：第一性原理
  ipo:
    input:   [现有认知框架, 待分析问题]
    process:
      steps: [识别并剥离所有假设, 回到不可再简化的基本元素, 从基本元素出发重新推导]
      tools: []  # 可选，指向实体
    output:  [去假设化的本质理解, 创新方案]
```

**方式 B：分解模型（组合性概念）**

适用于由多个子概念协同完成的方法/框架。用 `decomposition` 字段建模：

```
概念：深度思考
  decomposition:
    - step: 问题解构
      uses: concept://第一性原理
    - step: 多维分析
      uses: concept://维度思考
    - step: 模式匹配
      uses: concept://模式匹配
    - step: 洞悉本质
      uses: concept://洞悉本质
  output: [对复杂问题的本质性理解, 系统性解决方案]
```

**方式 C：混合模型**

一个概念既可以有自己的核心 IPO，同时也引用子概念完成特定步骤：

```
概念：知识体系构建
  ipo:                     # ← 整体加工逻辑
    input: [碎片化知识]
    process:
      steps: [收集整理 → 分类链接 → 抽象提炼 → 结构化输出]
    output: [个人知识体系]
  decomposition:            # ← 关键步骤引用子概念
    - step: 知识分类整理
      uses: concept://结构化分解
    - step: 抽象提炼
      uses: concept://从现象到抽象
    - step: 结构化输出
      uses: concept://结构化表达
```

> **设计原则**：不强制区分"原子"和"组合"，一个概念用哪种方式取决于其本身的内容特征。`ipo` 表达该概念自身的加工逻辑，`decomposition` 表达它对外部概念的引用组装。

### 1.4 概念与实体的边界

**核心判据**：能否被赋予一个完整的 IPO？

| 判据 | 概念 | 实体 |
|---|---|---|
| 有 IPO 吗？ | 有（或有 decomposition） | 没有，只有 define |
| 能独立加工？ | 是，输入→处理→输出 | 否，是被概念调用的资源 |
| 性质 | 思维加工方法/原理 | 具体的人、工具、产品、框架 |

**边界案例处理**：

| 对象 | 归类为 | 理由 |
|---|---|---|
| 麦肯锡七步法 | **实体**（模型/框架） | 它是麦肯锡公司的**具名方法论产品**，有明确的提出者、适用范围、步骤定义，本身不执行"输入→处理→输出"的思维加工 |
| SBR模型 | **实体**（模型/框架） | 它是**具名的可视化框架**，是一个工具而非一个思维加工过程 |
| 问题分析解决 | **概念** | 它是**通用思维动作**，具有 IPO 加工过程，不是具名产品 |
| 结构化思维 | **概念** | 它是**通用思维方法**，本身是加工过程 |
| Cursor | **实体**（工具） | 具体软件产品，没有 IPO |
| 知识管理 | **概念** | 通用活动领域，可赋予 IPO |

> 经验法则：如果你能把某物命名为"XX方法"且它是一个**具名的、由特定组织/人提出的方法论产品**→ 实体；如果它是**通用的思维加工动作**→ 概念。

---

## 二、YAML 文件 Schema 定义

知识元模型由 **3 个 YAML 文件**组成，存放在知识库根目录下的 `model/` 子目录中：

```
<知识库根目录>/
├── raw/                    # 历史文章（只读源材料）
├── model/
│   ├── scenarios.yaml      # 场景清单
│   ├── concepts.yaml       # 概念清单（唯一概念文件）
│   └── entities.yaml       # 实体清单（唯一实体文件）
└── README.md
```

### 2.1 通用约定

| 规则 | 说明 |
|---|---|
| **字段命名** | 全部 `snake_case` |
| **列表字段** | 用复数名（`sources`、`steps`、`tags`） |
| **单值字段** | 用单数名（`id`、`define`、`category`） |
| **必填字段** | 标注 `*` |
| **相对路径** | 所有 `sources` 使用相对于知识库根目录的路径，如 `raw/文章名.md` |
| **id 唯一性** | 同一 YAML 内 id 唯一；场景/概念/实体之间可重名（通过前缀区分） |
| **引用前缀** | `scenario://<id>`、`concept://<id>`、`entity://<id>` |
| **中文字段** | 不加引号；含 `:`、`#`、`-`、`{` 特殊字符时加引号 |
| **长文本** | 使用 YAML `\|` 块标量 |

### 2.2 scenarios.yaml — 场景

```yaml
# ============================================================
# scenarios.yaml — 场景清单
# 每个场景对应一个"如何..."的问题，定义触发条件与组装规则
# ============================================================

version: "2.0"                        # * schema 版本
updated_at: "YYYY-MM-DD"              # * 最近更新日期

scenarios:                            # * 场景列表

  - id: 如何构建个人知识体系            # * 场景唯一标识，格式为"如何…"
    sources:                           # * 来源文章（至少1篇），相对路径
      - raw/文章A.md
      - raw/文章B.md
    trigger: <string>                  # * 何时触发：用户提问或情境描述
    goal: <string>                     # * 要达成的目标（一段话，清晰描述输出）
    composition:                       # * 组装规则（至少1个 phase）
      - phase: <阶段名>                # * 阶段名称
        uses:                          # 该阶段调用的概念/实体
          - concept://<id>
          - entity://<id>
        rule: <string>                 # 该阶段的关键逻辑/约束（1-2句话）
        key_points:                    # 该阶段的关键要点（短句列表）
          - <string>
          - <string>
      - phase: <阶段名>
        uses:
          - concept://<id>
        rule: <string>
        key_points:
          - <string>
    inputs:                            # 场景总输入（可选但建议填写）
      - <string>
    outputs:                           # 场景总产出（可选但建议填写）
      - <string>
    related_scenarios:                 # 相关场景（标签式关联）
      - scenario://<id>
    tags: [<string>, ...]              # 分类标签
```

**字段约束**：

| 字段 | 约束 |
|---|---|
| `id` | 以"如何"开头，同一文件唯一 |
| `sources` | 至少 1 条有效相对路径 |
| `trigger` | 一句完整的话，描述"什么情况下会触发此场景" |
| `goal` | 一段完整的话，描述"此场景要达成的结果" |
| `composition` | 至少 1 个 phase；每个 phase 的 `uses` 至少引用 1 个概念或实体 |
| `composition[].uses` 中所有 `concept://` 和 `entity://` 引用必须能在对应 YAML 中解析 |

### 2.3 concepts.yaml — 概念

```yaml
# ============================================================
# concepts.yaml — 概念清单（唯一概念文件）
# 每个概念是一个可复用的思维加工单元，用 IPO 和/或 decomposition 建模
# ============================================================

version: "2.0"
updated_at: "YYYY-MM-DD"

concepts:                             # * 概念列表

  - id: <概念名>                       # * 唯一标识（中文名，不包含"概念""方法"等后缀）
    sources:                           # * 来源文章（至少1篇），相对路径
      - raw/文章A.md
      - raw/文章B.md
    define: <string>                   # * 一句话定义（精简准确，≤50字）
    type: <类型>                       # * 见下方枚举
    ipo:                               # IPO 模型（可选，与 decomposition 至少有一个）
      input:                           # 输入列表
        - <string>
      process:                         # 处理过程
        steps:                         # 处理步骤（有序）
          - <string>
        tools:                         # 使用的工具/技术实体（可选）
          - entity://<id>
      output:                          # 输出列表
        - <string>
    decomposition:                     # 分解模型（可选，与 ipo 至少有一个）
      - step: <步骤名>                 # * 步骤名称
        uses: concept://<id>           # * 引用的子概念
        why: <string>                  # 为何在此步骤使用该子概念（可选）
    relations:                         # 与其他概念/实体的关系
      is_a:                            # 父子归属
        - concept://<parent_id>
      depends_on:                      # 完全依赖（缺它不能成立）
        - concept://<id>
        - entity://<id>
      references:                      # 参考引用（理论基础或灵感来源）
        - concept://<id>
        - entity://<id>
      contains:                        # 包含（部分是整体的组成部分）
        - concept://<id>
      produces:                        # 产出（本概念的输出产物）
        - concept://<id>
        - entity://<id>
      uses:                            # 使用（工具/技术实体）
        - entity://<id>
      related_to:                      # 一般关联（兜底）
        - concept://<id>
        - entity://<id>
    tags: [<string>, ...]              # 分类标签

  # ---- 示例：纯 IPO 型概念 ----
  - id: 第一性原理
    sources:
      - raw/[2025-10-30]第一性原理模型图深刻理解创新的底层逻辑.md
    define: 回到事物最基本的、不可再简化的元素，从那里出发重新推导出结论的思维方法
    type: 思维方法
    ipo:
      input:
        - 现有认知框架（可能包含未经审视的假设）
        - 待分析的问题或现象
      process:
        steps:
          - 识别并剥离所有未经论证的假设（"真的是这样吗？"）
          - 层层追问，回到不可再简化的基本元素（物理/逻辑基础）
          - 从基本元素出发，按逻辑重新推导
        tools: []
      output:
        - 去假设化的问题本质理解
        - 基于基本原理的创新方案
    relations:
      references:
        - concept://还原论
        - concept://演绎逻辑
      related_to:
        - entity://马斯克
        - concept://洞悉本质
    tags: [思维方法, 创新, 本质思考]

  # ---- 示例：混合型概念（ipo + decomposition）----
  - id: 知识体系构建
    sources:
      - raw/[2021-11-15]为何要写一本思维框架和逻辑构建个人思维能力体系的书.md
      - raw/[2025-10-19]如何构建完整的个人知识体系以下五个关键点供你参考.md
    define: 将碎片化知识按照一定逻辑结构组织为系统的、可检索、可复用的知识架构的过程
    type: 知识管理
    ipo:
      input:
        - 碎片化的信息片段、学习笔记、实践经验
      process:
        steps:
          - 收集：将分散的知识源汇总到统一工作区
          - 分类：按知识领域和主题进行归类
          - 链接：建立知识点之间的交叉引用和关联
          - 抽象：从具体知识中提炼通用规律和模式
          - 结构化：形成树状或网状的知识结构
      output:
        - 结构化的个人知识体系
        - 可复用的知识组件
    decomposition:
      - step: 知识分类整理
        uses: concept://结构化分解
        why: MECE 原则确保分类不重不漏
      - step: 抽象提炼
        uses: concept://从现象到抽象
        why: 从具体知识上升到通用规律
      - step: 知识链接
        uses: concept://模式匹配
        why: 发现跨领域的共同模式
    relations:
      contains:
        - concept://结构化分解
        - concept://知识链接
      produces:
        - concept://可复用知识组件
      related_to:
        - concept://个人知识管理
        - entity://知识树
    tags: [知识管理, 结构化, 体系构建]

  # ---- 示例：纯 decomposition 型概念 ----
  - id: 解决问题完整流程
    sources:
      - raw/[2017-04-01]_问题分析和解决.md
      - raw/[2025-12-14]问题分析解决核心逻辑麦肯锡解决问题七步法仅是开始.md
    define: 从识别问题到输出解决方案的完整思维流程
    type: 思维方法
    decomposition:
      - step: 问题定义与边界划定
        uses: concept://问题定义
        why: 确定解决什么、不解决什么，避免范围蔓延
      - step: 问题结构化分解
        uses: concept://结构化分解
        why: 将大问题拆为可处理的子问题
      - step: 根因分析
        uses: concept://洞悉本质
        why: 从现象到根因，避免治标不治本
      - step: 方案假设与验证
        uses: concept://假设验证推理法
        why: 大胆假设、小心求证
      - step: 方案集成与评估
        uses: concept://分解与集成
        why: 将子方案整合为完整方案并评估
    relations:
      depends_on:
        - concept://问题定义
        - concept://结构化分解
      references:
        - entity://麦肯锡七步法
    tags: [问题解决, 方法论, 完整流程]
```

**`type` 枚举**：

| 类型 | 说明 | 示例 |
|---|---|---|
| `思维方法` | 通用的思维加工方式 | 归纳、演绎、抽象、第一性原理、结构化思维 |
| `认知原理` | 关于认知本身的规律 | 逻辑自洽、认知偏差、元认知 |
| `分析操作` | 具体的分析手法 | 分解、聚合、矩阵分析、对比分析 |
| `知识管理` | 知识获取/组织/应用 | 知识内化、知识萃取、知识归档 |
| `学习理论` | 学习相关的方法/原理 | 费曼学习法、间隔重复、刻意练习 |
| `管理学原理` | 管理与组织相关原理 | PDCA、目标管理、时间管理 |
| `哲学概念` | 哲学领域核心概念 | 辩证法、认识论、本体论 |
| `通用框架` | 非具名的通用方法论框架 | 如果框架是具名产品(如麦肯锡七步法)→归实体 |

**字段约束**：

| 字段 | 约束 |
|---|---|
| `id` | 中文名，不包含"概念""方法"等多余后缀。简洁精炼，如用"归纳"而非"归纳思维方法" |
| `sources` | 至少 1 条有效相对路径 |
| `define` | ≤50 字，一句话说清"这是什么" |
| `ipo` 和 `decomposition` | 至少存在一个，两者可共存 |
| `decomposition[].steps` | 至少 2 步（仅 1 步说明不需要分解） |
| `ipo.process.steps` | 至少 1 步 |
| `ipo.input` 或 `ipo.output` | 至少有一个非空 |
| 所有引用 | `concept://` 在本文件可解析；`entity://` 在 entities.yaml 可解析 |
| 关系优先级 | `is_a` > `depends_on` > `contains` > `uses` > `produces` > `references` > `related_to` |

### 2.4 entities.yaml — 实体

```yaml
# ============================================================
# entities.yaml — 实体清单（唯一实体文件）
# 每个实体是现实世界中可指认的具体对象
# ============================================================

version: "2.0"
updated_at: "YYYY-MM-DD"

entities:                             # * 实体列表

  - id: <实体名>                       # * 唯一标识
    sources:                           # * 来源文章（至少1篇），相对路径
      - raw/文章A.md
    category: <分类>                   # * 见下方枚举
    type: <类型>                       # * 见下方枚举
    define: <string>                   # 一句话定义（精简准确，≤50字）
    relations:                         # 关系（至少有一个关系才收录）
      is_a:                            # 父子归属（谁是其父类）
        - entity://<parent_id>
      depends_on:                      # 完全依赖（缺它不能运行）
        - entity://<id>
        - concept://<id>
      references:                      # 参考引用（理论基础或灵感来源）
        - entity://<id>
        - concept://<id>
      contains:                        # 包含（部分-整体关系）
        - entity://<id>
      produces:                        # 产出（该实体的输出产物）
        - entity://<id>
      uses:                            # 使用（工具/技术）
        - entity://<id>
      related_to:                      # 一般关联（兜底）
        - entity://<id>
        - concept://<id>
    children:                          # 子实体（最多3层）
      - id: <子实体名>
        category: <分类>
        type: <类型>
        define: <string>
        sources:
          - raw/文章C.md
        relations: { ... }
        children: []                   # 可嵌套到第3层
    tags: [<string>, ...]

  # ---- 示例 1：模型/框架型实体 ----
  - id: 麦肯锡七步法
    sources:
      - raw/[2021-03-03]问题分析和解决逻辑麦肯锡七步成诗仅是开始.md
      - raw/[2025-12-14]问题分析解决核心逻辑麦肯锡解决问题七步法仅是开始.md
    category: 框架与模型
    type: 模型/框架
    define: 麦肯锡公司提出的结构化解决问题方法论，包括问题陈述、问题分解、假设验证等步骤
    relations:
      uses:
        - concept://结构化思维
        - concept://金字塔原理
      related_to:
        - concept://问题分析解决
        - entity://问题四分类
    tags: [咨询方法论, 结构化思维, 问题解决, 麦肯锡]

  # ---- 示例 2：工具型实体 ----
  - id: Cursor
    sources:
      - raw/[2025-03-21]完整的AI辅助编写技术建议书或咨询方案的思路借助CursorClaude3.7.md
    category: 工具与平台
    type: 工具
    define: 基于VS Code的AI编程编辑器，内置Agent模式支持多文件编辑和上下文理解
    relations:
      uses:
        - entity://Claude
      related_to:
        - concept://AI辅助编程
        - entity://AI辅助工具
    tags: [AI编程, IDE, 开发工具, Agent]

  # ---- 示例 3：带子实体的层级实体 ----
  - id: 知识库
    sources:
      - raw/[2025-02-17]腾讯ima知识库横空出世不仅仅是知识库更是AI时代个人第二大脑.md
    category: 工具与平台
    type: 产品
    define: 用于知识存储、检索和管理的系统化工具
    relations:
      related_to:
        - concept://个人知识管理
        - concept://知识体系构建
    children:
      - id: 腾讯ima知识库
        sources:
          - raw/[2025-02-17]腾讯ima知识库横空出世不仅仅是知识库更是AI时代个人第二大脑.md
          - raw/[2025-02-22]_为何腾讯ima知识库是当前个人智能知识库搭建的最佳方式.md
        category: 工具与平台
        type: 产品
        define: 腾讯推出的AI原生知识库产品，支持智能检索、知识问答和内容生成
        relations:
          is_a:
            - entity://知识库
          uses:
            - entity://大语言模型
          related_to:
            - concept://个人知识管理
            - concept://AI辅助写作
      - id: Notion
        sources:
          - raw/[2025-02-22]_为何腾讯ima知识库是当前个人智能知识库搭建的最佳方式.md
        category: 工具与平台
        type: 产品
        define: 集成笔记、数据库、Wiki的项目管理和知识管理工具
        relations:
          is_a:
            - entity://知识库
          related_to:
            - concept://个人知识管理

  # ---- 示例 4：人物型实体 ----
  - id: 马斯克
    sources:
      - raw/[2025-10-30]第一性原理模型图深刻理解创新的底层逻辑.md
    category: 人物与组织
    type: 人物
    define: Tesla/SpaceX 创始人，第一性原理思维的标志性实践者
    relations:
      related_to:
        - concept://第一性原理
        - concept://洞悉本质
    tags: [第一性原理, 创新, 企业家]
```

**`category` 枚举**：

| 分类 | 说明 | 包含的 type |
|---|---|---|
| `工具与平台` | 软件、平台、应用、AI 工具 | 工具、产品、技术 |
| `框架与模型` | 具名的可视化或方法论框架 | 模型/框架 |
| `人物与组织` | 真实人物、公司、机构 | 人物、组织 |
| `知识资产` | 书籍、论文、课程、概念化的抽象对象 | 书籍、概念实体 |
| `技术与标准` | 底层技术、标准、协议 | 技术、标准 |

**`type` 枚举**：

| 类型 | 说明 | 示例 |
|---|---|---|
| `工具` | 软件、IDE、应用 | Cursor、Flowith、Anki |
| `产品` | 具名产品/服务 | ChatGPT、腾讯ima知识库 |
| `技术` | 底层技术/框架 | 大语言模型、向量检索、Kubernetes |
| `标准` | 标准/协议 | HTTP、BPMN 2.0 |
| `模型/框架` | 具名方法论框架模型 | SBR模型、麦肯锡七步法、金字塔原理 |
| `人物` | 真实人物 | 马斯克、亚里士多德 |
| `组织` | 公司/机构 | OpenAI、麦肯锡 |
| `书籍` | 正式出版物 | 《思考快与慢》《价值》 |
| `概念实体` | 实体化的抽象对象（兜底） | 个人知识体系、思维大厦 |

**字段约束**：

| 字段 | 约束 |
|---|---|
| `id` | 同一文件唯一 |
| `sources` | 至少 1 条有效相对路径 |
| `define` | ≤50 字 |
| `category` | 必须来自上述枚举，控制在 ≤2 层（顶层-子层） |
| `type` | 必须来自上述枚举 |
| 实体层级 | `children` 最多嵌套 3 层（level 1 → level 2 → level 3） |
| 子实体的 `is_a` | 必须包含其直接父实体 |
| 同一实体可有多个父 | 在 `is_a` 中列出全部 |
| 关系 | 实体至少有一个 `relations` 条目，否则说明该实体孤立、不值得收录 |

---

## 三、关系类型定义

关系是连接概念与概念、概念与实体、实体与实体的"语义胶水"。共定义 **8 种关系**，按精确度优先级排列：

### 3.1 关系一览

| # | 关系 | 英文 | 语义 | 方向 | 典型例句 |
|---|---|---|---|---|---|
| 1 | 父子归属 | `is_a` | A 是 B 的一种/子类 | 子→父 | "归纳是一般化思维方法的一种" |
| 2 | 完全依赖 | `depends_on` | A 离开 B 无法成立或运转 | 下游→上游 | "演绎的结论可靠性 depends_on 前提为真" |
| 3 | 包含 | `contains` | A 由 B 组成，B 是 A 的组成部分 | 整体→部分 | "知识体系构建 contains 知识分类整理" |
| 4 | 使用 | `uses` | A 使用工具/技术/资源 B | 调用者→被调用者 | "深度思考 uses SBR模型（作为分析工具）" |
| 5 | 产出 | `produces` | A 的输出/产物是 B | 过程→产物 | "知识萃取 produces 可复用知识组件" |
| 6 | 参考引用 | `references` | A 的理论基础或灵感来源于 B，但不依赖 B 才能成立 | 引用者→被引者 | "个人知识管理 references 知识管理理论" |
| 7 | 一般关联 | `related_to` | A 与 B 有语义关联但不符合以上任一种 | 无方向 | "第一性原理 related_to 价值投资" |
| 8 | 实例化 | `instance_of` | A 是 B 的一个具体实例（仅 entity→concept 方向） | 实例→概念 | "麦肯锡七步法 instance_of 问题解决方法论" |

### 3.2 关系判定决策树

```
实体A 是 实体B 的子类/一部分？
  ├─ 是，且是分类学意义上的"是一种" → is_a
  └─ 否 ↓

实体A 离开 实体B 还能正常运转吗？
  ├─ 不能，B 是 A 成立的必要条件 → depends_on
  └─ 能 ↓

实体B 是 实体A 的组成部分？
  ├─ 是，B ∊ A → contains
  └─ 否 ↓

实体A 把实体B 当作工具使用？
  ├─ 是 → uses
  └─ 否 ↓

实体B 是 实体A 的直接产物？
  ├─ 是 → produces
  └─ 否 ↓

实体A 的理论灵感或思想来源引用自 B（但非依赖）？
  ├─ 是 → references
  └─ 否 ↓

实体A 是 概念B 的具体实例？
  ├─ 是（仅适用于 entity→concept） → instance_of
  └─ 否则 → related_to（兜底）
```

### 3.3 关系使用规范

| 规范 | 说明 |
|---|---|
| **优先精确** | 能 `depends_on` 就不要 `references`，能 `contains` 就不要 `related_to` |
| **双向检查** | 如果 A `contains` B，则 B 不应再 `contains` A（禁止循环包含） |
| **is_a 传递性** | 如果 A `is_a` B 且 B `is_a` C，则 A 隐式 `is_a` C（不需要显式写出） |
| **概念 vs 实体引用** | `concept://` 和 `entity://` 可以在各层之间双向引用（如概念 `uses` 实体，实体 `references` 概念） |
| **跨层引用** | 场景 → 概念 ✓、场景 → 实体 ✓、概念 → 实体 ✓、实体 → 概念 ✓、概念 → 场景 ✗（场景不能被概念引用） |

---

## 四、去重规范

wiki1 实践中最大的教训就是**粒度过细、近义重复**。例如：

- "主动思考""独立思考能力""独立思维""独立思考意识"——4 个概念表达几乎相同的意思
- "知识广度""知识深度""知识广度与深度"——单独定义 + 组合定义，重复
- "思维框架""思维框架逻辑""思维框架与内在逻辑"——标题微调造成的冗余

### 4.1 去重判定标准

以下情况应判定为 **重复**，必须合并：

**A. 同义概念（≥80% 语义重叠）**

| 原始概念 A | 原始概念 B | 合并后 |
|---|---|---|
| 独立思考能力 | 独立思维 | **独立思考**（取最简洁的表达） |
| 知识广度 | 知识深度 | 分别保留，但"知识广度与深度"作为组合概念删掉（内容在各自的 IPO 中已经覆盖） |
| 思维框架 | 思维框架逻辑 | **思维框架**（"逻辑"是思维框架的内在属性，不需要单独成概念） |

**B. 粒度拆分过细**

一个完整概念被拆成了 2-3 个过细的概念：

| 原始拆分 | 合并策略 |
|---|---|
| "目标制定" + "目标分解" + "目标跟踪" | 合并为 **目标管理**，子步骤放在 ipo.process.steps 中 |
| "静态分析" + "动态分析" + "动静结合" | 保留"静态分析"和"动态分析"（有独立 IPO），删掉"动静结合"（它是两者的组合使用方式，应在场景或 decomposition 中表达） |

**C. 名称变体**

同一概念因文章中的不同表述产生了多个 id：

| 变体 | 合并为标准 id |
|---|---|
| 学习复盘、复盘总结、学习实践复盘 | **学习实践复盘** |
| 结构化表达、结构化呈现、结构化输出 | **结构化表达** |

### 4.2 同名概念去重流程

在两轮萃取的第一轮末尾执行：

```
1. 将所有候选概念按语义聚类（用 define 和 tags 辅助判定）
2. 同一聚类内的概念：
   a. 选择中文名最简洁精炼的作为标准 id
   b. 合并所有 sources（去重后）
   c. 选择 define 最精确的那条（或从多条中提炼一条 ≤50 字的）
   d. 将其他概念中独有的 relations / tags 合并入标准概念
3. 删除被合并的概念
4. 更新所有被合并 id 的引用为新的标准 id
```

### 4.3 应该保留独立概念的情况

以下情况即使名称相似，也应 **保留为独立概念**：

| 情况 | 示例 | 理由 |
|---|---|---|
| 有显著不同的 IPO | "归纳" vs "演绎"：IPO 路径完全不同 | 加工逻辑不同 |
| 分属不同领域但有相同名称 | 编程中的"抽象" vs 认知中的"抽象"：本质相同，合并 | IPO 相同时合并 |
| 组合概念有独特的 decomposition | "深度思考"独有 4 步 decomposition，不能合并入其子概念 | 组装逻辑独立 |

### 4.4 颗粒度控制原则

**概念颗粒度自检**：一个概念应该满足——

1. **有独立的 IPO 或 decomposition**：如果一个概念只有 `define` 没有 `ipo` 也没有 `decomposition`→ 它可能不需要作为独立概念存在，可以考虑合并到父概念中
2. **能在场景中被单独调用**：如果一个概念永远不会被某个场景直接 `uses`，而总是作为另一个概念的步骤出现 → 考虑合并
3. **define 与其他概念不重复**：如果两句话表达的意思 80% 相同 → 合并

---

## 五、两轮萃取流程

### 5.1 总体流程

```
┌─────────────────────────────────────────────────────┐
│                   第一轮：快速扫描                    │
│                                                       │
│  按批次读取 raw/ 文章 → 提取核心要素 →               │
│  记录候选 场景/概念/实体 → 不去重、不展开 IPO       │
│                         ↓                             │
│  全量扫描完成后 → 综合分析 → 去重合并 →              │
│  形成最终的《场景清单》《概念清单》《实体清单》       │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│                   第二轮：内容填充                    │
│                                                       │
│  按清单逐条填充完整内容：                             │
│  场景：trigger / goal / composition / ...            │
│  概念：define / ipo / decomposition / relations / ...│
│  实体：define / relations / children / ...           │
│                                                       │
│  填充时回查 raw/ 原文，确保所有内容有出处             │
└─────────────────────────────────────────────────────┘
```

### 5.2 第一轮：快速扫描（提取清单）

**目标**：产出候选的场景/概念/实体清单（只要 id + 一句话 define + sources），不做深入填充。

**批次大小**：每批 5-10 篇文章。

**每批操作**：

1. 读取 5-10 篇 raw/ 文章
2. 从每篇文章中识别：
   - **场景**：文章试图回答的"如何..."问题。一篇文章可能包含 0-多个场景
   - **概念**：文章中提出的思维方法、认知原理、分析操作。拿不准时问自己："这个能写成 IPO 吗？"
   - **实体**：文章中提到的具体人物、工具、产品、框架模型
3. 以**表格形式**追加记录到进度文件（`model/_round1_progress.md`）

**进度文件格式**：

```markdown
# 第一轮萃取进度

## 批次 1 — YYYY-MM-DD
- 处理文件：raw/文章A.md, raw/文章B.md, ...
- 候选场景：
  | id | define（一句话） | sources |
  |---|---|---|
  | 如何XXX | ... | 文章A.md |
- 候选概念：
  | id | define（一句话） | sources |
  |---|---|---|
  | 概念A | ... | 文章A.md |
- 候选实体：
  | id | category | type | define（一句话） | sources |
  |---|---|---|---|---|
  | 实体A | 工具与平台 | 工具 | ... | 文章A.md |

## 批次 2 — YYYY-MM-DD
...
```

**全量扫描完成后的综合去重**：

1. 将所有候选场景/概念/实体汇总到一个临时文件
2. 按 [第四节去重规范](#四去重规范) 执行去重
3. 产出：
   - `model/scenarios.yaml` — 仅含 `id` + `sources` + `define`（其他字段标注 `TODO`）
   - `model/concepts.yaml` — 仅含 `id` + `sources` + `define` + `type`
   - `model/entities.yaml` — 仅含 `id` + `sources` + `category` + `type` + `define`
4. 将此清单提交给用户确认

### 5.3 第二轮：内容填充

**目标**：基于确认后的清单，逐条回查原文，填充完整的 IPO、decomposition、relations 等。

**每批操作**：

1. 选取清单中的 5-10 条概念（或 5-10 条场景、5-10 条实体），定位到其 `sources` 中的原文
2. 仔细阅读原文，提取：
   - **概念**：完整 IPO（原文中找"前提/输入 + 步骤 + 结果/输出"）、子概念关系、跨概念引用
   - **场景**：触发条件（原文中找"为什么需要解决这个问题"）、goal（原文中找"解决后能得到什么"）、composition（阶段划分 + 每阶段用到的概念）
   - **实体**：关系网络（原文中找"使用了什么""依赖什么""与什么相关"）、父子层级
3. 写入对应的 YAML 文件
4. 所有内容必须有原文依据，不能凭空编造
5. 更新 YAML 文件头部的 `updated_at`

### 5.4 萃取原则

| 原则 | 说明 |
|---|---|
| **原文忠实** | 概念的 define、IPO、decomposition 内容必须来自原文，不能由 AI 自由发挥 |
| **宁缺毋滥** | 找不到明确 IPO 的概念不强行填充，标注为仅 define（在备注中说明） |
| **粒度适中** | 参考 4.4 节的自检标准，避免过细拆分 |
| **相对路径** | 所有 sources 使用 `raw/文件名.md` 格式 |
| **跨文件一致性** | 填充时如发现某个 `concept://` 引用指向不存在的 id，同步补建或修正 |
| **不过度引用** | 每个概念的 `relations` 控制在 2-6 条（避免像 wiki1 中某些概念挂了 10+ 个 related_to） |

---

## 六、建模规范指南

### 6.1 概念建模规范

**规范 1：`define` 写作规则**

```
✓ 好：回到事物最基本的、不可再简化的元素，从那里出发重新推导出结论的思维方法
✗ 差：第一性原理是一种非常重要的思维方法，很多成功人士都在使用，它可以帮助我们更好地创新
```
- 只说"是什么"，不说"有多重要""谁在用""有什么用"
- ≤50 字
- 使用原文中的核心措辞

**规范 2：`ipo` 的完整度要求**

一个合格的 IPO 应满足：
- `input` 明确列出加工前的原材料（不是模糊的"信息""数据"）
- `process.steps` 是完整的、有序的操作序列（不是并列的想法罗列）
- `output` 是加工后的产物，与 `input` 对应

```
✓ 好例子：
  概念：目标分解
  ipo:
    input:  [总目标, 可用资源清单]
    process:
      steps:
        - 将总目标拆解为子目标（MECE原则）
        - 为每个子目标分配资源和优先级
        - 制定子目标的可衡量标准
        - 验证子目标加总后覆盖总目标
    output: [目标分解树, 资源分配计划]

✗ 差例子：
  概念：目标分解
  ipo:
    input: [需求]
    process:
      steps: [分析, 分解, 执行]
    output: [解决方案]
```
→ 问题：input 太模糊，steps 是关键词罗列而非步骤序列，output 不对应。

**规范 3：`decomposition` 的步骤粒度**

- 每个 step 对应一个清晰的动作，而不是一个宽泛的领域
- `uses` 应指向一个**具体的概念**，而不是一个模糊的标签
- 步骤数量 2-6 个

```
✓ 好：
  decomposition:
    - step: 问题定义与边界划定
      uses: concept://问题定义
    - step: 结构化拆解
      uses: concept://结构化分解
    - step: 方案生成
      uses: concept://假设验证推理法

✗ 差：
  decomposition:
    - step: 思考  ← 太模糊
      uses: concept://思维  ← 太宽泛
```

**规范 4：概念 `type` 分配**

| 如果你的概念是… | type 选… |
|---|---|
| 如何想/如何推理的方法 | `思维方法` |
| 关于人如何认知的规律/发现 | `认知原理` |
| 具体的分析步骤/操作 | `分析操作` |
| 知识相关的获取/组织/应用 | `知识管理` |
| 学习相关的方法/原理 | `学习理论` |
| 管理/组织/效率相关 | `管理学原理` |
| 哲学领域的核心术语 | `哲学概念` |
| 以上都不匹配的框架性概念 | `通用框架` |

### 6.2 实体建模规范

**规范 5：实体的边界判断**

一句话判据：**能说"我用了 XX"但 XX 不是思维动作本身的 → 实体。**

| 表述 | 归类 | 理由 |
|---|---|---|
| "我用了 MECE 原则分析问题" | 概念 | MECE 是分析操作（有 IPO） |
| "我用了麦肯锡七步法解决此问题" | 实体 | 麦肯锡七步法是具名方法论产品 |
| "我用归纳法得出结论" | 概念 | 归纳是思维动作（有 IPO） |
| "我用 Cursor 写了这段代码" | 实体 | Cursor 是具体工具 |

**规范 6：实体层级控制**

- 实体采用 `children` 嵌套，最多 3 层
- 层级决策：如果一个子实体有独立的 `sources` 和 `define`，且能被其他概念/实体独立引用 → 放 `children`
- 如果子实体只是父实体的一个属性/功能 → 不要拆分，在父实体的 `define` 中一句话带过

**规范 7：实体的 `relations` 控制**

- 每个实体至少 1 个 `relations` 条目（否则说明该实体孤立、不值得收录）
- 每个实体不超过 6 条关系（避免关系泛滥）
- `is_a` 是必须优先填写的（如果有分类学父级）

### 6.3 场景建模规范

**规范 8：`id` 表述**

- 必须以"如何"开头
- 表述一个具体的、可操作的问题
- 不要太宽泛（"如何提升自己"）也不要太窄（"如何在周二下午三点用Cursor写Python代码"）

```
✓ 如何进行深度思考
✓ 如何构建个人知识体系
✓ 如何应对AI时代的认知鸿沟
✗ 如何思考（太宽泛）
✗ 深度思考方法论（不是问句形式）
```

**规范 9：`composition` 的阶段设计**

- 每个 phase 名称是一个名词短语（不是句子）
- phase 之间的顺序从输入到输出、从简单到复杂、从理论到实践
- 每个 phase 的 `rule` 是 1-2 句话，描述该阶段的**关键约束或组装逻辑**

**规范 10：`trigger` 和 `goal` 的写作**

```
trigger ← 一段话，回答"用户处于什么状态/提出什么问题时会触发这个场景"
goal   ← 一段话，回答"经过这个场景的处理后，用户能得到什么"
```

- 不要写成抽象的定义，要写成具体的场景描述
- `trigger` 举例："用户面临大量碎片化信息，不知道如何系统地组织和管理个人知识，提出'如何搭建个人知识体系'或类似问题"
- `goal` 举例："产出覆盖知识收集、分类、链接、抽象、结构化五步的完整知识体系构建方案，并推荐适合的工具实体"

### 6.4 通用建模规范

**规范 11：`sources` 相对路径格式**

```
✓ raw/文章标题.md
✓ raw/[2025-10-30]第一性原理模型图深刻理解创新的底层逻辑.md
✗ E:/BaiduDownload/llm-wiki/raw/文章标题.md  （禁止绝对路径）
✗ raw\文章标题.md                              （禁止反斜杠）
✗ ./raw/文章标题.md                             （禁止 ./ 前缀）
```

**规范 12：引用完整性检查清单**

每次修改 YAML 后：
- [ ] 本文件内所有 `concept://` 或 `entity://` 引用是否都能在同文件或对应文件中解析？
- [ ] 新增条目是否与已有条目存在 ≥80% 语义重叠？
- [ ] `sources` 路径是否真实存在？
- [ ] `updated_at` 是否已更新？
- [ ] 新增条目是否满足各自的最小字段要求？

---

## 七、应用指南（提示词模板）

以下提示词模板可直接用于驱动 AI 执行知识萃取。每个模板中的 `<占位符>` 需替换为实际值。

### 7.1 第一轮快速扫描提示词

#### 模板 1-A：启动第一轮扫描

```
## 任务

你正在执行知识元模型的第一轮萃取（快速扫描阶段）。你的角色是**内容提炼助手**，不是内容创作者。

## 背景

我们有一套三层知识元模型：
- **场景**：回答"如何..."的问题，是概念的组装
- **概念**：可复用的思维加工单元，有 IPO（输入→处理→输出）或可分解为子步骤
- **实体**：具体的人、工具、产品、框架、模型

## 输入

知识库根目录：`<知识库根目录>`
原始文章目录：`<知识库根目录>/raw/`
建模规范文档：`<知识库根目录>/知识萃取规范指南.md`

## 操作

1. 读取建模规范文档，理解 Schema 和萃取原则
2. 按顺序读取 `<知识库根目录>/raw/` 下的文章，每次处理 5-10 篇
3. 对每篇文章，提取：
   - **场景**：文章试图回答的"如何..."问题（0-多个）
   - **概念**：文章中提出的思维方法/认知原理/分析操作。判断标准：能否写出 IPO？
   - **实体**：文章中提到的具体人物/工具/产品/框架模型。判断标准：能被"使用了XX"引用？
4. 只提取 **id + define + sources**，不展开 IPO 和 decomposition
5. 将结果追加到 `<知识库根目录>/model/_round1_progress.md`

## 输出格式

在 `_round1_progress.md` 中按以下格式记录：

| id | define（一句话，≤50字） | sources |
|---|---|---|
| ... | ... | raw/文章名.md |

## 重要规则

- define 必须来自原文措辞，不是你的概括
- 拿不准归类的要素，优先放概念
- 宁可多提取（后续去重会合并），不可遗漏
- 每个 sources 使用相对路径：raw/文件名.md
- 当前批次：第 <N> 批，处理文件：<文件列表>
```

#### 模板 1-B：综合去重（第一轮收尾）

```
## 任务

第一轮快速扫描已完成。现在需要汇总所有候选要素并进行去重合并。

## 输入

- 进度文件：`<知识库根目录>/model/_round1_progress.md`
- 建模规范：`<知识库根目录>/知识萃取规范指南.md`（重点阅读第四节"去重规范"）

## 操作

1. 汇总进度文件中所有批次的候选场景、概念、实体
2. 执行去重（按建模规范第四节的标准）：
   - 同义概念（≥80% 语义重叠）→ 合并
   - 名称变体 → 统一为最简洁 id
   - 粒度拆分过细 → 合并为更粗粒度概念
3. 产出最终清单（仅含 id + define + sources + type/category）

## 输出

将去重后的清单写入：
- `<知识库根目录>/model/scenarios.yaml`  ← 仅骨架（id/sources/define/tags）
- `<知识库根目录>/model/concepts.yaml`   ← 仅骨架（id/sources/define/type/tags）
- `<知识库根目录>/model/entities.yaml`   ← 仅骨架（id/sources/category/type/define/tags）

未填充的字段标注 `# TODO: 待第二轮填充`

## 输出后

向用户报告：
- 去重前/后的数量变化（场景：X→Y，概念：X→Y，实体：X→Y）
- 被合并的主要概念对（A+B→C）清单
- 请用户确认清单后再进入第二轮
```

### 7.2 第二轮内容填充提示词

#### 模板 2-A：填充概念

```
## 任务

你正在执行知识元模型的第二轮萃取（内容填充阶段）。请为以下概念补充完整内容。

## 输入

- 待填充的概念 id 列表：`<概念1>, <概念2>, ...`
- 概念骨架文件：`<知识库根目录>/model/concepts.yaml`
- 建模规范：`<知识库根目录>/知识萃取规范指南.md`
- 原始文章：每个概念的 sources 字段指向的 raw/ 文章

## 操作

对每个概念：
1. 定位到其 sources 指向的原始文章
2. 通读文章中的相关段落
3. 填充以下字段（均需有原文依据）：
   - `define`：如骨架中已有则保持/优化，确保 ≤50 字
   - `ipo`：从原文中找"前提/输入 → 步骤 → 结果/输出"的完整链路
   - `decomposition`：如果该概念在原文中表现为多个子步骤，且每个子步骤对应已有概念
   - `relations`：与其他概念/实体的关系
   - `tags`：关键词标签
4. 写到 concepts.yaml 的对应条目

## IPO 提取技巧

在原文中寻找以下信号：
- "首先…然后…最后…" → steps
- "需要…为前提"、"基于…" → input
- "得到…"、"产出…"、"形成…" → output
- "需要用到…工具"、"借助…" → process.tools

## 重要规则

- 所有内容必须以原文为依据，禁止凭空编造
- 如果原文确实找不到完整的 IPO，只填能找到的部分，在备注中标注
- 概念之间的引用（concept://）必须能解析
- relations 控制在 2-6 条
- 更新文件头部的 updated_at
```

#### 模板 2-B：填充场景

```
## 任务

为以下场景补充完整的 composition（阶段组装规则）。

## 输入

- 待填充的场景 id 列表：`<场景1>, <场景2>, ...`
- 场景骨架文件：`<知识库根目录>/model/scenarios.yaml`
- 概念清单：`<知识库根目录>/model/concepts.yaml`
- 实体清单：`<知识库根目录>/model/entities.yaml`
- 建模规范：`<知识库根目录>/知识萃取规范指南.md`

## 操作

对每个场景：
1. 回查其 sources 原文，理解原文对该问题的回答逻辑
2. 设计 composition（3-6 个 phase），每个 phase：
   - `phase`：阶段名称（名词短语）
   - `uses`：该阶段需要的概念/实体（从 concepts.yaml 和 entities.yaml 中选择）
   - `rule`：该阶段的关键约束（1-2 句，原文中有依据）
   - `key_points`：关键要点（短句，原文中有依据）
3. 补充 trigger / goal / inputs / outputs

## 阶段设计原则

- 从"为什么"到"是什么"到"怎么做"到"怎么验证"
- 每个 phase 的 uses 必须是 concepts.yaml 或 entities.yaml 中存在的 id
- phases 之间有逻辑递进，不是简单罗列
```

#### 模板 2-C：填充实体

```
## 任务

为以下实体补充完整的关系网络和子级层级。

## 输入

- 待填充的实体 id 列表：`<实体1>, <实体2>, ...`
- 实体骨架文件：`<知识库根目录>/model/entities.yaml`
- 建模规范：`<知识库根目录>/知识萃取规范指南.md`

## 操作

对每个实体：
1. 回查其 sources 原文，提取：
   - `define`：如骨架中已有则保持/优化
   - `relations`：原文中提到的与其他实体/概念的关系（按 8 种关系判定）
   - `children`：原文中明确提到的子级实体
2. 控制 relations 在 1-6 条
3. 控制 children 层级在 3 层以内

## 关系提取技巧

| 原文表述 | 对应关系 |
|---|---|
| "XX 是一种 YY" | is_a |
| "XX 必须依赖 YY 才能运行" | depends_on |
| "XX 包含 YY" | contains |
| "使用 XX 工具" | uses |
| "XX 产生/输出 YY" | produces |
| "XX 参考/借鉴了 YY" | references |
```

### 7.3 新增内容提示词

#### 模板 3：回答新问题并回写元模型

```
## 任务

基于知识元模型回答用户问题。如果问题无法匹配已有场景，则创建新场景并回写。

## 输入

- 用户问题：`<用户提问>`
- 知识库根目录：`<知识库根目录>`
- 元模型文件：model/scenarios.yaml, concepts.yaml, entities.yaml

## 操作流程

1. 理解用户问题，提取核心意图
2. 在 scenarios.yaml 中检索匹配的场景（按 trigger / goal / tags 匹配）
3. 如果命中 → 按 composition 组装答案
4. 如果未命中或不完全匹配：
   a. 从 concepts.yaml + entities.yaml 中检索相关概念和实体
   b. 按 IPO 自行编排答案
   c. 回答完毕后，将本次的"场景 + 组装路径"候选回写：
      - 新场景写入 scenarios.yaml（按 Schema 填齐必填字段）
      - 如果使用了 concepts.yaml 中不存在的概念 → 判断是否需要新增
      - 所有新增内容标注 sources 指向当前对话（或后续补写）
5. 报告使用了哪些概念和实体，是否新增了场景
```

---

## 附录 A：wiki1 常见问题与改进对照

| wiki1 的问题 | 本规范的改进 |
|---|---|
| 概念分为 meta + compose，两个文件，概念边界模糊 | **一个 concepts.yaml**，用 `ipo` 和 `decomposition` 区分建模方式，不再强制二分 |
| 实体分 5 个文件，type 枚举文件中不一致 | **一个 entities.yaml**，用 `category` + `type` 统一分类 |
| 665 个原子概念，大量近义重复 | **严格去重标准**（80%语义重叠→合并），**两轮迭代**（先出清单去重，再填充） |
| 部分概念只有 define 没有 IPO，虚概念多 | **颗粒度控制原则**：无 IPO 无 decomposition 的概念不应独立存在 |
| 关系类型 4 种（is_a / uses / depends_on / related_to），概念-实体语义模糊 | **8 种精确关系**，增加 contains / produces / references / instance_of |
| schema 定义和 prompt 混在 profile.md 中 | **本规范双轨**：前半部分建模规范，后半部分提示词模板 |
| 一次生成，无去重机会 | **强制两轮**：R1 仅提 id+define→去重确认→R2 完整填充 |
| sources 无路径规范 | **统一 raw/前缀的相对路径** |

---

## 附录 B：最小检查清单

每次萃取完成后逐项检查：

### B.1 结构检查

- [ ] 仅 3 个 YAML 文件（scenarios / concepts / entities）
- [ ] 所有 `sources` 使用 `raw/` 相对路径
- [ ] 所有 `concept://` 引用可在 concepts.yaml 解析
- [ ] 所有 `entity://` 引用可在 entities.yaml 解析
- [ ] 所有 `scenario://` 引用可在 scenarios.yaml 解析
- [ ] `updated_at` 已更新

### B.2 去重检查

- [ ] 没有两个概念的 define 表达 80% 相同语义
- [ ] 没有名称变体重复（如 A思维 / A思考 / A意识 → 合并为 A）
- [ ] 没有过细拆分的概念（如目标制定/目标分解/目标跟踪 → 合并为目标管理）

### B.3 概念质量检查

- [ ] 每个概念都有 `ipo` 或 `decomposition`（或两者都有）
- [ ] 每个概念的 `define` ≤ 50 字
- [ ] 每个概念的 `relations` 在合理范围内（0-6 条）
- [ ] `ipo.input`、`ipo.process.steps`、`ipo.output` 来自原文，不是 AI 编造

### B.4 实体质量检查

- [ ] 每个实体至少有 1 个 `relations` 条目
- [ ] `children` 嵌套 ≤ 3 层
- [ ] 子实体的 `is_a` 包含直接父实体

### B.5 场景质量检查

- [ ] 每个场景的 id 以"如何"开头
- [ ] 每个场景有 `trigger` 和 `goal`
- [ ] 每个场景的 `composition` 至少 1 个 phase
- [ ] 每个 phase 的 `uses` 引用的 id 都在 YAML 中可解析

---

*本规范基于 wiki1（v1.0）实践经验总结升级至 v2.0。核心改进：结构简化（8 文件→3 文件）、严格去重、两轮迭代、IPO 与 decomposition 混合建模。*
