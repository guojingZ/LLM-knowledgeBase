# 2026-09-23

## 知识元模型 · 第一轮扫描（50 篇 raw/ 文章）

- 规范：`知识萃取规范指南.md` v2.0（场景/概念/实体三层 + 8 种关系 + 两轮萃取）
- 语料：raw/ 50 篇（972KB），全部为人月聊IT 何明璐的思维/认知/知识管理系列长文
- 执行：批次 1 由我人工示范抽取 4 篇；批次 2-8（46 篇）派 7 个并行子代理抽取
- 产物：
  - `model/_round1_rubric.md` — 统一抽取判据（三层判别、概念/实体边界、粒度红线、输出模板）
  - `model/_round1_progress.md` — 批次 1 示范结果
  - `model/_round1_batch2.md` ~ `_round1_batch8.md` — 各批次候选
  - `model/_round1_stats.py` — 候选汇总统计脚本（可重跑）
- 候选量：场景 72 / 概念 390（去重 325）/ 实体 247（去重 182）/ 存疑项 50

## 关键发现（待第二轮处理）

- 场景冗余率 0% 是**坏信号**：72 个场景 id 无一重复，说明同一问题被不同批次用不同措辞表达，去重压力最大
- 概念冗余 17%、实体 26%；`模式匹配` 被 7 个批次提出，是全局枢纽概念
- 概念词簇最密集：`知识`(47)、`思维`(24)、`认知`(15)、`学习`(13) → 第二轮去重主战场
- 概念 vs 实体边界易错点已固化进 rubric：通用思维动作→概念；具名方法论/可视化框架/书籍/人物→实体

## 流程约定（用户要求）

- 用户要"展露中间过程"并亲自把关，所以每批次结果都落盘成独立 md，不做自动合并
- 第一轮只做候选登记，**去重合并需用户确认后再执行**（规范里去重属第一轮收尾，但用户明确要先看候选）

## 第一轮收尾：综合去重（已完成）

用户对 3 条存疑项的裁决（已生效）：
1. 静态结构/动态结构 → 是「结构化思维」的内部步骤，不独立成概念
2. 因果自洽/关系自洽/过程自洽 → 是「逻辑自洽」的三个维度（第二轮写进 decomposition）
3. 满意解/幅度决策 → 并入「系统思维」的 IPO 描述

**由裁决①推导出通用规则**：静态/动态成对出现的结构性描述一律不独立成概念。据此连带合并了
结构化认知、动静融合、动静融合分析、动静分析融合、从静态到动态、静态分析、动态分析。

新增产物：
- `model/_round1_merge.py` — 汇总 8 批 → `_round1_candidates.json` + `_round1_all_candidates.md`
- `model/_round1_dedup_map.md` — **去重决策审计表**（每条合并/删除都写理由，可直接改后重跑）
- `model/_round1_build_yaml.py` — 按决策表生成三个骨架 YAML
- `model/scenarios.yaml` 17 条 / `concepts.yaml` 234 条 / `entities.yaml` 163 条（语法校验通过）

去重结果：场景 72→17（−76%）、概念 325→234（−28%）、实体 182→163（−10%）
- 概念降幅小的原因：325 条里有 182 条从未被合并规则触及（各篇独有术语），是第二轮重点
- 校验结果：50 篇文章全部被引用、无失效路径、无空 define

**可复用做法**：dedup_map 作为"决策与代码分离"的中间层——改决策表即可重跑生成，不用改脚本。
生成后必须跑校验（规则 id 是否命中候选、是否有漏网场景），否则写错的 id 会静默失效。

## 第二轮内容填充（已完成）

流程：
1. `_round2_rubric.md` — 填充规范（IPO 质量红线、8 种关系、引用可解析、宁缺毋滥标"证据不足"）
2. `_round2_plan.py` — 按「主源文章所属批次」把 234 概念 + 163 实体分 8 组，产出 `_round2_taskN.md`
3. 批次 1（27 概念 + 25 实体）由主 agent 亲自填（原文已在上下文）；批次 2-8 派 7 个子代理并行
4. 子代理写 `_round2_partN.yaml` 片段（**不直接改主文件，避免并发写冲突**），再由 `_round2_merge.py` 合并
5. 场景 17 条单独分 3 组填，由 `_round2_scen_merge.py` 合并

最终成果：场景 17 / 概念 234 / 实体 163，全部填充完毕
- IPO 步骤总计 958 步，关系引用 1638 条，**失效引用 0**
- 附录 B 全项通过：场景 id 全部"如何"开头、概念均有 ipo/decomposition、实体均有 relations、sources 全部有效
- 3 条标注"证据不足"：呈现和泛化、知识关联、问题抽象（原文支撑太薄，宁缺毋滥）

**踩过的两个坑（重要）**：
1. 主 agent 自己写的 relations 用了第一轮已被合并掉的旧 id（`学习实践复盘`→应为`复盘`、`entity://MECE`→应为`concept://MECE`）——**引用校验必须跑，人也会犯**
2. `_round2_merge.py` 的 block() 辅助函数把子字段缩进写成 4 空格（应为 6），导致 `relations:`/`ipo:` 被 YAML 解析成 null、子字段变成平级键。**数据没丢但结构全坏**，外观看不出来，只有校验能发现
   → 教训：**脚本生成 YAML 后必须 safe_load 回来验证结构，不能只看文件文本**
   → 修复后需先重跑 `_round1_build_yaml.py` 重建干净骨架，再重跑合并（否则会把脏数据当骨架）
